# 因果ループプラグイン

## 説明
astah*のユースケース図で作成した因果ループのループを抽出表示するプラグインです。
因果関係を読み解く時の助けになるツールです。

# ダウンロード
- [ここ](https://github.com/snytng/inga/releases/latest)からダウンロードして下さい。

# インストール
- ダウンロードしたプラグインファイルをastah*アプリケーションにドラッグドロップするか、Program Files\asta-professionals\pluginsに置いて下さい。

# 機能説明
- 特別なルールで記述したユースケース図を因果ループ図として解析します。
- プラグインの表示エリアに、自己強化ループ・バランスループを一覧表示します。
- プラグインの表示エリアをクリックするとクリックしたループを強調表示します。

## 使い方

### 解析
- ユースケース図で因果ループを作成します。（ユースケースをノードとして、＋を関連線（矢印付き実線）、－を依存線（矢印付き点線）として追加します）
- ingaプラグインを開きます。（プラグインタブを選択します）
- 因果ループの解析結果が表示されます。<img src="https://raw.githubusercontent.com/snytng/inga/master/images/2022-10-09%20231940.png" alt="解析結果表示" width="50%">

### 表示
- 解析結果リストの行を選択すると関連する図の要素が選択されます。
- 因果ループの解析結果の行を押すと、因果のリンクや、ループを色を付けて強調表示します。
  - 自己強化ループは緑色で表示します。<img src="https://raw.githubusercontent.com/snytng/inga/master/images/2022-10-09%20232015.png" alt="解析結果表示" width="50%">
  - バランスループは赤色で表示します。<img src="https://raw.githubusercontent.com/snytng/inga/master/images/2022-10-09%20232039.png" alt="解析結果表示" width="50%">
  - 選択したリンクやノードはマゼンタで表示します。<img src="https://raw.githubusercontent.com/snytng/inga/master/images/2022-10-09%20232106.png" alt="解析結果表示" width="50%">
- 図の要素を選択すると関連する解析結果だけを表示します。

### 設定
- `リンク彩色`で編集中、解析結果の選択時にリンクやノードを色付けを選択できます。
- `リンク記号`で表示で利用する増減記号を選択できます。
- `リンク定義`でユースケース図上で因果の定義を選択できます。
  - Defaultは実線（関連）と破線（依存）です。
  - それ以外は関連の関連名の文字列で判断します。

## 免責事項
このastah* pluginは無償で自由に利用することができます。
このastah* pluginを利用したことによる、astah*ファイルの破損により生じた生じたあらゆる損害等について一切責任を負いません。

## 変更履歴
- V0.3.5
    - SO,+-,同逆のリンクで双方向依存を因果と判断しないように修正
- V0.3.4
    - リンクを選択してもシミュレーションを実行する機能を追加
    - ノードを選択したときに処理が停止する不具合を修正
    - 開発ツールをeclipseからVisual Studio Codeに変更
- V0.3.3
    - 編集中のリンクに色を付ける機能を追加
    - 解析結果を選択すると図を色付けする機能を追加
    - オプションを保存する機能を追加（[user.home]/.astah-inga.properties）
- V0.3.2
    - 編集しているユースケース図で因果ループを強調表示する機能を追加
    - 追加のユースケース図の作成して因果ループを表示する機能を削除
- V0.3
    - ユースケース図での因果の定義を実線（関連）と破線（依存）以外にも選択できる機能を追加（SO,+-,同逆）
    - 増減記号を選択できる機能を追加
    - 選択したノードを増加したときのシミュレーション結果を表示する機能を追加
- V0.2.1
    - 選択したノート、リンクを先頭にしてループ表示するように変更
- V0.2
    - ループの自己強化、バランス判定の不具合を修正
    - ノードのループ表示を追加（総ループ数、自己強化ループ数、バランスループ数）
- V0.1.3
    - 解析結果を選択すると関連する図上の要素を選択するように変更
    - 図の要素を選択したときに関連する解析結果を表示するように変更
- V0.1.2
    - 因果ループを一番上に表示するように変更
    - ノードとリンクの属性を元の図面に合わせて表示するように変更
- V0.1.1
    - 因果ループのみを表示するように変更
- V0.1
    - 初版

以上
